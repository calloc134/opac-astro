---
import Layout from "../layouts/Layout.astro";
import Card from "../components/Card.astro";
import json from "../../output.example.json";

const calcRemainingDays = (return_date: string) => {
  const today = new Date();
  const returnDate = new Date(return_date);
  const diffDays = Math.ceil(
    (returnDate.getTime() - today.getTime()) / (1000 * 60 * 60 * 24)
  );
  return diffDays;
};
---

<Layout title="Welcome to Astro.">
  <main>
    <section class="flex flex-col items-center h-screen">
      {
        json.map(({ detail, return_date, image_url }) => (
          <Card
            title={detail}
            return_date={return_date}
            image_url={image_url}
            remaining_days={calcRemainingDays(return_date)}
          />
        ))
      }
    </section>
  </main>
</Layout>
